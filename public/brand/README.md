# HORONHO Brand Assets

Replace `horonho-logo.svg` with the official logo file when available.

Recommended production names:

- `horonho-logo.svg` for vector logo.
- `horonho-logo.png` for the official raster logo if SVG is unavailable.

Keep the header dimensions similar so the layout remains stable.
